# Hariri_task
Code to create the schedule and stimuli for a Hariri_task using python.

This repository contains:
1) a script with helper functions that should be loaded as a module, 
2) a script that takes generates stimuli for each trial for the block faces of the task
3) a script that generates a counterbalanced scehdule regarding the faces blocks 
